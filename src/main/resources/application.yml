spring:
  datasource:
    url: jdbc:mariadb://localhost/sample
    driverClassName: org.mariadb.jdbc.Driver
    username: sa
    password: password
  sql:
    init:
      mode: embedded

  logging:
    level:
      root: info
